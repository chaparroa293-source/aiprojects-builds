# Build log

## 2026-09-08 — Foundation initialized

- Created the project foundation as an ordinary folder in the parent `aiprojects-builds` repository on `main`.
- Added the documentation skeleton, profession notes, empty source/database directories, and secret-safe `.gitignore`.
- Clarified the distinction between product truth, code truth, and data truth.
- Left frontend, package manager, database, authentication, deployment, and GitHub remote unselected.
- No product workflow, UI, schema, dependency stack, infrastructure connection, or deployment was created.

## 2026-09-08 — Slice 1: Client Directory

- Added the React + Vite application, Spanish (`es-PY`) client directory UI, and Supabase client integration.
- Added the minimal `clients` migration and `.env.example`; no credentials or Supabase project configuration were added.
- Authentication, RLS policy, deployment, and all modules beyond Client Directory remain deferred.

## 2026-09-08 — Shared Supabase namespace correction

- Corrected the unexecuted Client Directory migration and client calls to use the dedicated `practice_management` schema in the shared AI Builds Supabase project.
- No Supabase credentials, connection, migration execution, or live database action occurred.

## 2026-09-08 — Slice 1 persistence verified

- Applied the scoped migration to the shared AI Builds project, creating only `practice_management.clients` and its local timestamp function/trigger.
- Exposed only `practice_management` and `clients` through the Data API; automatic exposure for future tables is off.
- Verified fake-client create, direct database row, directory/detail read, edit, and browser reload persistence through the browser-facing `anon` role.
- Authentication and production RLS remain deferred; this is development/testing only.

## 2026-09-08 — Slice 1 checkpoint

- Removed the single fake persistence-verification client after confirming create, read, edit, reload, filter, and sort behavior; the directory returned to its empty state.
- Client Directory remains development-only: browser access is limited to `SELECT`, `INSERT`, and `UPDATE`; authentication, ownership, RLS, and production hardening are deferred.

## 2026-09-08 — Slice 2: Session history

- Applied the scoped `practice_management.sessions` migration with a direct `client_id` foreign key, status constraint, optional duration, client-history index, and the existing scoped `updated_at` behavior.
- Exposed only `sessions` in the existing `practice_management` Data API namespace and granted the development browser role only `SELECT`, `INSERT`, and `UPDATE`.
- Verified fake client/session create, empty history, no-duration entry, all allowed states, direct database read, edit, and reload persistence. Scoped fake records were removed after verification.

## 2026-09-08 — Slice 3A: Appointment foundation + Client Agenda

- Confirmed the live scoped `practice_management.appointments` table, FK, constraints, indexes, and `updated_at` trigger against the local migration; no migration rerun was needed.
- Explicitly exposed only `practice_management.appointments` through the existing Data API configuration; automatic exposure remains off. The development `anon` role has only `SELECT`, `INSERT`, and `UPDATE` on appointments; an unexpected DELETE privilege was revoked and confirmed absent.
- Verified fake client/appointment empty Agenda, create, direct ownership and NULL-duration read, edit, reload persistence, UI validation, no client-reassignment control, and database rejection of an unsupported status. Scoped fake records were removed and confirmed absent.
- Global Agenda, Appointment → Session conversion, Payments, authentication, and production RLS remain unimplemented.

## 2026-09-09 — Slice 4: Client Payments

- Applied the scoped `practice_management.payments` migration with a direct `client_id` foreign key, `numeric(14,0)` positive-amount constraint, client-payment index, and the scoped `updated_at` trigger.
- Explicitly exposed only `practice_management.payments` through the existing Data API configuration; automatic exposure remains off. The development `anon` role has only `SELECT`, `INSERT`, and `UPDATE`; it has no `DELETE` grant.
- Verified a fake client’s empty Pagos state, required/non-positive validation, create, blank notes as `NULL`, direct ownership, PYG formatting and total, edit, and reload persistence. The two fake payments and matching fake client were removed and confirmed absent.
- Global Payments, authentication, and production RLS remain unimplemented.

## 2026-09-09 — PMS-S04: Session ↔ Payment allocation

- Confirmed the live `practice_management.sessions.payment_id` nullable column, `ON DELETE SET NULL` foreign key, same-client trigger, and reverse-read index.
- Added client-scoped Session link/change/unlink capture. A Session reads its nullable `payment_id`; a Payment reads linked Sessions through that foreign key and derives its linked-session count without changing payment amount or Total received.
- Verified with disposable records: NULL compatibility, link and reload read, one Payment linked to two Sessions, change, unlink, cross-client rejection, `ON DELETE SET NULL`, and reverse counts. All disposable clients, sessions, and payments were removed; a final database count returned zero.
- No allocation amounts, invoices, balances, debt, reconciliation, global Sessions, or global Payments were introduced.

## 2026-09-09 — PMS-S05: Quick Capture

- Added the persistent shell entry for compact Session and Payment capture using existing domain create operations and tables only.
- Verified disposable Quick Session (`payment_id = NULL`) and Quick Payment creation, direct retrieval, blank Payment notes as `NULL`, and a ₲ 5.000 payment total input. The test client and its children were removed; the final client count was zero.
- Production build passed and the strict PMS UI/static audit returned zero findings. No Quick Capture object, history, allocation, balance, or global module was added.

## 2026-09-10 — Product Definition Gate reconciliation

- Established `docs/PRODUCT.md` as the compact Product Definition Gate for a Spanish-first, Paraguay-oriented, single-practitioner Practice Management System and recorded the Capture → Organize → Schedule → Communicate → Operate → Remember → Understand product loop.
- Reconciled durable summaries with the implemented PMS-S05 state and kept `TECHNICAL_SPEC.md` limited to the implemented Client, Appointment, Session, and Payment domain grammar.
- Preserved broader Directory, calendar and Google Calendar integration, communication, follow-up, notes, analytics, access-control, hosting, and PWA direction as unresolved product design rather than speculative objects or implementation commitments.
- No application code, database, migration, Supabase configuration, dependency, or runtime behavior changed.

## 2026-09-10 — PMS-CAL-002: Calendar v1

- Added Calendario as a first-class Monday–Sunday weekly view over existing Appointments, with Anterior/Hoy/Siguiente navigation, URL-backed week persistence, day-prefilled creation, Client selection, edit/reschedule/cancel, and Calendar ↔ Client Agenda navigation.
- Reused the existing Appointment form, validation, create/update operations, and stored model. Added only the inclusive cross-client Appointment date-range read; no schema, migration, configuration, dependency, or Appointment semantic change was required.
- Kept cancelled Appointments visible and labelled, kept Client ownership locked on edit, and added explicit loading, empty, saving, error, and demo behavior. Wide screens use seven day columns; tablet and phone layouts group days vertically without horizontal grid scrolling.
- Browser verification covered previous/current/next weeks, Monday and Sunday boundaries, Calendar create/edit/reschedule/cancel, Client Agenda create/edit, both navigation directions, selected-week reload, persisted values, and unchanged Quick Capture fields. Desktop, tablet landscape, tablet portrait, and narrow phone widths had no page-level horizontal overflow.
- Production build, strict frontend audit, and `git diff --check` passed. The disposable client and both Appointments were removed after verification; independent final counts returned zero Clients, Appointments, Sessions, and Payments and zero matching verification records.

## 2026-09-10 — PMS-UX-002: Distinctive Light Workspace refinement (implementation candidate, pending user visual review)

- Peer-review MEDIUM findings resolved. M1: Calendar and Client Agenda now share one `persistAppointment` helper for Appointment writes; each keeps its own list reconciliation; no Appointment semantics changed. M2: Agenda-level "Ver en calendario" navigates to the week of the nearest upcoming Appointment, else the most recent past one, else the current week (`relevantWeekIso`); the per-Appointment link still opens that Appointment's week. Docs reconciled.
- Interaction system: introduced a reusable right-side `Drawer` (light scrim, eyebrow + title, explicit `×` "Cerrar", one primary save, `Cancelar` secondary, Escape/scrim close, first-field focus, ~440px / full-width ≤560px). Add/Edit Client and every Appointment create/edit (Calendar and Client Agenda) now open in the drawer with the workspace visible behind. The Calendar's full-surface form and the "Cerrar sin guardar" label were removed. Sessions, Payments, and Quick Capture deliberately left inline.
- Visual system moved to "Distinctive Light Workspace": token set re-based to charcoal ink on white / soft cool off-white with a pale blue-grey shell and a single restrained blue accent; serif display type replaced by one geometric-humanist sans stack with page titles reduced from ~4rem to ~1.5rem; sidebar de-weighted to a light rail; Clientes de-cluttered (removed nested bordered surfaces, unboxed the toolbar, suppressed the empty detail column so the directory is full-width until a client is selected); Calendar header compressed to one row (segmented Anterior/Hoy/Siguiente group + `Nueva cita` primary), per-day "Agregar cita" demoted to low-weight blue text, appointment cards calmed (hairline cards, status dot, no fills/bars), "today" changed from the focus-ring style to a subtle tint + "Hoy" pill.
- Accessibility touch-ups on surfaces already in scope: week navigation is a `<nav>` group, the drawer close control has an accessible label, the main heading is focused on view change.
- No schema, migration, dependency, Supabase configuration, Appointment semantic, Quick Capture semantic, or persisted-object change. `package.json` / `package-lock.json` / `database/` unchanged.
- Verification in demo mode (`hasSupabaseConfig` false) across desktop, tablet landscape/portrait, and 375px: Add/Edit Client drawer, Agenda and Calendar Appointment drawers (create, edit with locked client, cancel — cancelled card stays visible), week navigation + URL sync + reload persistence on a non-current week, Agenda→relevant-week and per-Appointment→specific-week navigation, Sessions/Payments/Quick Capture unaffected. Production build and `git diff --check` pass; no page-level horizontal overflow; fresh-tab console clean. No persistent test data created (demo mode only).

## 2026-09-10 — PMS-UX-003: user visual-review corrections (implementation candidate, pending user visual review)

- **Calendar "Agregar cita" defect.** Root cause: the per-day action was a ~26px transparent left-aligned text sliver — a poor pointer target and a sub-44px touch target (tablet-first), and when zero Clients existed it rendered `disabled` (greyed, inert). Not an overlap or handler bug (`elementFromPoint` returned the button; the handler opened the drawer when invoked). Fix: a real dashed-outline button, min 36px, `display:flex`, solid hover — verified with actual `computer` mouse clicks in the stacked layout and a real bubbling pointer/mouse/click sequence in the 7-column layout; both open the day-prefilled Appointment drawer via the shared `persistAppointment` path with the week board preserved. When no Client exists the per-day actions and `Nueva cita` are replaced by an enabled `Agregar cliente` (toolbar + guidance-banner button) that navigates to Clientes and opens the Client create drawer.
- **Sidebar collapse.** Added an explicit `☰` control in the rail header (`aria-expanded`, `aria-controls`, labelled "Contraer/Expandir navegación", distinct from the drawer `×`). ≥821px: toggles a 64px icon-only rail (labels/brand/account text hidden, three destinations still reachable). ≤820px: shows/hides the wrapped nav strip. Choice persists in `localStorage`; defaults to collapsed on narrow; auto-collapses after navigating on narrow. Verified at desktop / tablet-landscape / tablet-portrait / phone.
- **Quick Capture → Nuevo cliente.** Contextual `＋ Nuevo cliente` beside the Client selector opens the existing Client create drawer over the still-mounted Quick Capture; on save the drawer closes, the new Client is auto-selected (`quickClientId` → effect in `QuickCapture`), and every entered Session/Payment value is preserved. Client stays its own object via the normal create path. Verified: entered notas/hora/duración intact, new Client selected, appears in the list.
- **Client contact actions.** `ClientDetail` phone → `tel:` link + `Llamar` / `WhatsApp` (`https://wa.me/<n>`, new tab) chips; email → `mailto:` link. `waNumber` normalises digits and local `0` → `595` for the URL only; stored `telefono` is untouched. Links only — no messaging/history/automation. Verified resolved hrefs (`tel:+595981555284`, `https://wa.me/595981555284`, `mailto:…`).
- **Directory header sorting.** `Cliente` and `Estado` `<th>` are now buttons with `aria-sort`; click toggles asc/desc, active column shows a chevron and others a neutral `⇅`. Default `nombre` asc. The `Ordenar` dropdown was removed (its recency option is dropped — the directory is searchable/filterable and small). Verified asc/descending toggle and that `aria-sort` is set only on the active column.
- **Supporting palette.** Added muted `amber` / `teal` / `lavender` / `coral` (+ `-soft` tints) used only on chips, status dots, small accents and tinted info cards: teal = active client / completed session / `Total recibido` card / WhatsApp chip; amber = scheduled appointment + "today" tint & "Hoy" pill; coral = cancelled; lavender = Quick Capture module accent + client initials. Large surfaces stay neutral; blue remains the sole primary-action colour.
- **Practitioner identity.** The rail account block is a documented placeholder (`practitioner` constant, `data-placeholder`, comment) shaped for a future authenticated profile; no auth/profile/workspace modelling added.
- **PMS-UX-002 preserved:** Client & Appointment right drawers, explicit `×`, context preservation, light workspace, compact one-row Calendar header, weekly Calendar semantics, M1 `persistAppointment`, M2 relevant-week navigation, tablet-first responsive — all re-verified.
- No schema, migration, dependency, Supabase configuration, Appointment/Session/Payment/Quick Capture semantic, or persisted-object change. `package.json` / `package-lock.json` / `database/` unchanged. Production build and `git diff --check` pass. Verified in demo mode across desktop / tablet-landscape / tablet-portrait / phone with no page-level horizontal overflow; fresh-tab console clean; no persistent test data created. The zero-Client Calendar path was verified by code path + its verified constituents (client drawer open, navigation), not against a live empty database.

## 2026-09-10 — PMS-UX-004: operational detail refinement + deletion audit (implementation candidate, pending hand test)

- **Deletion audit — no deletion implemented.** For Client / Appointment / Session / Payment: (A) the browser `anon` role has no `DELETE` grant on any of the four tables (`docs/ENVIRONMENT.md`, `docs/DATABASE.md`, migration comments) and `client-api.js` exposes no delete function; (B) FKs — `appointments.client_id`, `sessions.client_id`, `payments.client_id` → `clients(id)` with no `ON DELETE` clause (`NO ACTION`), and `sessions.payment_id` → `payments(id)` `ON DELETE SET NULL`; (C) so a hard Client delete is blocked by any related row, and only a Payment delete has a defined consequence (linked Sessions' `payment_id` cleared, Session kept — `REL-004`); (D) `TECHNICAL_SPEC.md` OPERATIONS lists Delete `Unsupported` for all four and `UX-CONTRACT.md` states no deletion exists — deletion is not defined by current docs; (E) hard delete could orphan `Total recibido` history (Payment) or leave a Client's Agenda/Sessions/Pagos referencing a gone parent; (F) each object already has a softer transition — Appointment/Session `estado='cancelada'`, Client `estado='inactivo'`. Conclusion: no object is "clearly safe and already supported" for deletion. Added an explicit **DEFERRED → Deletion** block to `TECHNICAL_SPEC.md` and a known-unknown to `docs/PRODUCT.md` with the per-object decision a future approved slice must make (hard delete vs. state transition, FK behavior, Payment-history handling, required `GRANT`/RLS). No delete controls, no API, no schema change.
- **Duration control.** New `DurationField` — preset pills 30 / 45 / 60 / 90 min above the existing free-entry number `input` (name still `duracion_minutos`). Preset click sets the value; clicking the active preset clears it; a typed non-preset value is kept verbatim (no coercion, no closed dropdown); still optional. Used in `AppointmentForm`, `SessionForm`, and Quick Capture's session form — one pattern. Verified: each preset, toggle-off, manual `75`/`60`, blank; saved through both the state path (Session/Quick) and the `FormData` path (Appointment).
- **Estado semantic colour.** Extended the existing supporting palette consistently: Client Detail Estado is now a `.status` chip (was plain text); each form's Estado `<select>` gets a 3px left border in the current value's colour (`activo`/`completada` → teal, `programada` → amber, `cancelada` → coral, `inactivo` → muted). Table and card chips already carried these colours (PMS-UX-003) and are unchanged. Text labels retained everywhere; no saturated form/card surfaces.
- **Session → Payment navigation.** `Pago registrado` on a Session with a linked Payment is now a teal `Pago registrado →` button that switches to that Client's Pagos tab and reveals the exact Payment (teal outline + `aria-current` + `scrollIntoView`). Reveal clears on tab change / opening another payment / selecting another client. If the linked Payment is absent from the loaded set the label is plain muted text (no dead link); a stale click shows `El pago asociado ya no está disponible.`. Uses the existing `sessions.payment_id`; smallest UI state added is one `revealedPaymentId`. Verified live: click reveals the "06 sept. 2026 · ₲ 150.000" row.
- **Contact-link consistency.** Audited every place phone/email is displayed: Client Detail (rich — already done), Client Directory rows (this pass — `tel:` / `mailto:` on the cell value, readable formatting kept, no chips), Quick Capture `<option>` text (N/A — inside a `<select>`). Verified Directory hrefs `mailto:ana.benitez@ejemplo.com` and `tel:+595981555284`.
- **PMS-UX-002/003 preserved:** light workspace, blue primary hierarchy, amber/teal/lavender/coral supporting palette, right drawers + explicit `×`, context-preserving create/edit, `☰` nav collapse, sortable Client headers, Quick Capture → Nuevo cliente, Client Detail contact actions, compact Calendar header, weekly Calendar semantics, M1 shared Appointment persistence, M2 relevant-week navigation, tablet-first — all intact.
- No schema, migration, dependency, Supabase config, or domain-semantic change. `package.json` / `package-lock.json` / `database/` / `src/supabase.js` / `.env.example` unchanged. Production build and `git diff --check` pass. Verified in demo mode (desktop / tablet-landscape / tablet-portrait / phone), no page-level horizontal overflow, fresh-tab console clean, no persistent test data. The dead-link Session→Payment branch was verified by code inspection (the live link path was exercised).

## 2026-09-10 — PMS-UX-005: reference-driven visual polish (implementation candidate, pending user visual sign-off)

- CSS-only pass (`src/styles.css`; `App.jsx` untouched). No visual references were attached; worked from the written principles — quiet light canvas, dense structured information, restrained semantic colour, one high-contrast anchor.
- **Density / typography.** Page title `clamp(1.25–1.4rem)`, section heads `1rem`, description → `.84rem` muted; compact metadata scale where hierarchy is carried by weight + colour, not size. Workspace padding, page-header, section gaps, form field gaps, tab spacing, toolbar, and drawer padding all tightened ~20–35%. More content visible before scroll; tablet targets preserved.
- **Tables / lists.** Directory rows ~36–40px (phone no longer wraps); header a quieter tinted strip with `.63rem` labels; selected row = soft tint + 3px blue inset accent (moved off the whole `<tr>` background). Session / Agenda / Payment records converted from individually bordered cards to hairline-separated rows inside one quiet frame; hover is a faint wash; a revealed Payment = teal tint + 3px teal inset accent (replaces the outer ring).
- **Pagos `Total recibido`** promoted to the single high-contrast anchor: dark `ink` block, white ~1.55rem total, muted label + secondary explanation, 3px `teal` inset accent. No invoice/balance/debt meaning added.
- **Status chips** unified: full pills → small `sm`-radius rounded rectangles, `.7rem`/600, soft tint + 1px tint-family border, one line. Calendar cards keep the dot+text variant of the same colours.
- **Quick Capture** tightened (padding 24→16/18, field gaps, 60px notes) to feel fast; **duration presets** raised to 40px touch targets and, in a `.form-grid`, the `DurationField` (and the Estado field after it) span the full row so all four presets sit on one line — no more wrapped pills.
- **Drawer** header/body/footer padding tightened, close control 32px, scrim `.22 → .24`, shadow slightly deeper; width `440 → 430px`. Reads as a contextual pane, not a modal.
- **Sidebar / Calendar** spacing, nav-item padding, day-header hierarchy, appointment-card padding, and the "Hoy" marker (pill → small square) tightened. Weekly IA, header structure, navigation, semantics all unchanged.
- Preserved: navigation IA, right-side drawer + `×` + close behaviour, context-preserving create/edit, `☰` collapse, sortable headers, Quick Capture → Nuevo cliente, Calendar weekly model + header + navigation, Session → Payment reveal, contact links, duration/status/payment semantics, M1/M2. No new objects, workflows, Calendar modes, deps, or DB/API/config change.
- Verified in demo mode across desktop / tablet-landscape (1024) / tablet-portrait / 375px: Clientes, Client Detail, Agenda, Sesiones, Pagos, Captura rápida, Calendario (7-col and stacked), Client and Appointment drawers. Duration presets still pick/toggle/manual-enter correctly. No page-level horizontal overflow; fresh console clean. Production build and `git diff --check` pass; JS bundle byte-identical (no logic change).

## 2026-09-10 — PMS-UX-006: darker sidebar + expanded semantic colour system (implementation candidate, pending final visual sign-off)

- CSS/token-only pass (`src/styles.css`; `App.jsx` untouched, JS bundle byte-identical). No layout, density, workflow, or component change — UX-005 structure preserved.
- **Sidebar → dark navy anchor.** `--color-rail` repointed to `--navy #1a2333` (cool blue-charcoal, not green, not black). New sidebar-scoped tokens: `--nav-text` (labels, 12:1), `--nav-muted` (secondary), `--nav-icon` (quieter inactive icons), `--nav-accent #5b9bff` (active edge + focus on dark), `--navy-raised` (active panel), translucent-white hover/edge. Active nav = `navy-raised` panel + 3px blue edge (not a saturated block). `☰` toggle, brand square (kept blue), Quick Capture (lavender-tinted rail treatment), and account/footer all re-themed for dark. Collapsed 60px rail and the ≤820px strip (now a dark bar with a soft shadow, blue underline for active) use the same treatment.
- **Expanded palette, tokenised into families** — each `strong-fg / soft-bg / tint-edge`: navy (shell + anchors), blue (action/link/selection/focus), teal (completed·received·positive·saved — `--color-success` now aliases teal), amber (scheduled·today·planned; `--amber-wash` for the today column), coral (cancelled·errors·destructive — `--color-danger` folded into the coral family), lavender (Quick Capture + contextual accents), peach (demo/info notices — `--color-warn-*` now aliases peach), sky (secondary info surfaces). ~9 near-duplicate ad-hoc hexes replaced by named tokens.
- **Applied where meaning supports it:** demo banner → peach; saved `notice` → teal; `error-message` → coral edge; Pagos `Total recibido` → `--navy-anchor` (lighter than the rail, still teal-accented, supporting text lightened for AA); `linked-session-summary` + `locked-client` → sky; Quick Capture → pale lavender wash + lavender selected-mode toggle + lavender `＋ Nuevo cliente` (inputs forced back to white; **save stays blue**); today calendar column → softer `--amber-wash`. Calendar/Clientes/Sesiones status colours unchanged in meaning, only tightened (teal darkened `#0F7D78 → #0B6F6A` for a 5.2:1 chip).
- **Accessibility.** Verified WCAG contrast in-browser: sidebar label 12.2:1, active label 12.8:1, inactive icon 5.1:1, brand caption 5.6:1, Quick Capture rail text 9.6:1; teal chip / `Pago registrado` 5.2:1; peach demo text 5.0:1; blue link 6.4:1; white total on `navy-anchor` 12.8:1. Status still always carries its text label (and a dot in calendar cards).
- Drawers stay light; primary action stays blue; button hierarchy unchanged; no destructive button styles introduced (no destructive action exists).
- Verified in demo mode at desktop / tablet-landscape (1024) / tablet-portrait / 375px: expanded + collapsed sidebar, hamburger, active nav, Quick Capture nav, account/footer, drawer + dark sidebar together, Calendario, Clientes/Datos, Sesiones, Pagos, Quick Capture. No responsive structure changed; no page-level horizontal overflow; fresh console clean. Production build and `git diff --check` pass.

## 2026-09-10 — PMS-UX-007: connected records + surface polish (implementation candidate, pending real-data hand test)

- **Connected-record audit.** Every persisted object presented as a distinct record already opened its edit context on click (Client row → detail; Agenda/Calendar Appointment → edit drawer; Session → edit form; Payment → edit form; `Pago registrado →` → linked Payment reveal). No dead-end records were found. This pass makes that discoverability explicit: each row `<button>` gets an `aria-label` ("Editar sesión del …", "Editar cita de …", "Editar pago del …"), a stronger hover (`primary-soft` + right inset accent) and visible `:focus-visible`. Contact `<a>` links stay in their own cells — clicking them never selects the row (verified). No nested interactive markup.
- **Edit actions.** Client Detail keeps its explicit compact `Editar`. Session/Appointment/Payment rows rely on click + aria-label (a per-row button would break the dense list); their edit context shows an "Editar …" eyebrow. No large primary Edit CTA anywhere.
- **Lifecycle language.** One-line notes added to the Client, Appointment, and Session forms: hard delete does not exist; use `Inactivo` (Client) / `Cancelada` (Appointment, Session). Payment has no void/delete UI and none is implied.
- **Nueva cita → Nuevo cliente.** `AppointmentForm` (new-mode, `showClient`) gains a `＋ Nuevo cliente` beside the Client select — same pattern as Quick Capture. It opens the Client create drawer stacked on top of the still-mounted appointment drawer (`Drawer` gained an `escapeClose` prop; the outer drawer disables its Escape while the nested one is open, so the draft survives). On save the client drawer closes, the new Client is auto-selected in the appointment, and all entered Appointment values are preserved. Works from the toolbar `Nueva cita` and from day-specific `Agregar cita`. One shared `newClientId` state feeds both Quick Capture and the calendar `AppointmentForm`. No second Client form.
- **Deletion — still deferred (unchanged from PMS-UX-004).** No DELETE grants, no delete API, no hard delete. Objects still requiring a future explicit deletion-policy slice: **Payment** (only object with no soft lifecycle state — needs a void/reversal decision); **Client / Appointment / Session** have soft lifecycle states (`inactivo` / `cancelada`) but a hard-delete policy + FK behaviour + `GRANT`/RLS is still an open decision.
- **Surface / shadow system.** New `--shadow-1` / `--shadow-2` tokens (subtle border + low-opacity shadow). L1 applied to the directory table, Client-detail workspace panel, Calendar frame, Session/Payment list frame, Quick Capture. L2 = calendar-appointment hover lift + selected/revealed row inset accent. L3 = drawer (unchanged).
- **Client Detail = one contained workspace.** `.detail-panel` is now an L1 surface; `.detail-tabs` is a tinted header strip rounded into the panel top; record lists inside drop their own shadow and bleed to the panel edges (dividers, not nested boxes). Datos rows get slightly more padding.
- **Calendar planner feel.** Toolbar gets a faint tint header; day columns use a 1px grid gap for clean separation (replacing per-day borders) + more padding; Today keeps amber but gains a 3px amber inset accent bar for presence; appointment cards get `--shadow-1` + a 3px status-colour left edge (amber `programada` / coral `cancelada`) and lift to `--shadow-2` on hover — real interactive objects, status colour still restrained.
- Colour system, typography, IA, navigation, weekly Calendar model, drawer close semantics, duration/status/payment semantics, M1/M2 — all unchanged. No schema/migration/dependency/API/delete change. `App.jsx` + `styles.css` only; `database/` / `package*.json` / `client-api.js` (delete) untouched.
- Verified in demo mode at desktop / tablet-landscape (1024) / tablet-portrait / 375px: row/card → edit for Client, Session, Payment, Agenda & Calendar Appointment; `Pago registrado →` → exact Payment; Nueva cita → Nuevo cliente (toolbar + day-specific) with values preserved and nested-Escape keeping the draft; Calendar Today + hover; contained tab workspace on Datos/Agenda/Sesiones/Pagos. No page-level horizontal overflow; fresh console clean. Production build and `git diff --check` pass.

## 2026-09-10 — PMS-TEST-001: real-data hand test & release readiness

- **Scope:** verification only. No code changes during the pass. Environment: connected Supabase (`practice_management` schema, browser `anon` role), real app on `http://127.0.0.1:5173` (Vite dev server serving the current uncommitted working tree), **not demo mode** (`hasSupabaseConfig` true, no demo banner). Baseline before the test: 2 client rows (1 pre-existing user record "Agus Ch"; 1 relabelled probe), 0 appointments/sessions/payments.
- **Result: all flows pass against real persistence.**
  - Clients: create, reload-persist, directory listing, search, `Estado` filter (activo/inactivo/todos), row-select → contained Datos/Agenda/Sesiones/Pagos workspace, Edit drawer, edit reload-persist, `tel:` / `mailto:` / `wa.me` links (`0981 123 456` → `tel:+595981123456`, `wa.me/595981123456` new tab), clicking a contact link does **not** trigger row-select/edit, lifecycle note ("…cambiá su estado a Inactivo; no hay eliminación de registros").
  - Appointments / Agenda: create with duration presets (30/45/60/90) and custom value (no coercion), edit, reschedule (16→18→22 Sep across weeks), status → Cancelada (stays visible), Client ownership not editable in the Agenda form, row opens edit, `Ver en calendario` → relevant week. Reload-persist confirmed.
  - Calendar: correct Monday–Sunday week, previous/today/next (async re-fetch from Supabase), selected week survives reload (`#calendario?week=…`), real Appointment on the correct day, card is a `<button>` with `aria-label` and status text + coral/amber colour, Today = amber wash + "Hoy" chip + amber inset bar, cross-week reschedule shows the "Navegá a esa semana" notice and moves the record, Calendar↔Agenda navigation both directions.
  - **Nueva cita → Nuevo cliente (critical regression):** draft entered → `+ Nuevo cliente` → Client drawer stacks above the Appointment drawer → new disposable Client created and saved → Client drawer closes, Appointment drawer stays open, new Client auto-selected, **all draft values (date/time/60-min preset/notes) preserved** → Appointment saved → reload: both Client and Appointment persisted on the correct day. Same from day-specific `Agregar cita` (selected day stayed prefilled). Escape / Cancelar / scrim on the nested drawer each close **only the top drawer** and keep the Appointment draft; no inaccessible controls.
  - Sessions: two created (multi-record list with hairline dividers inside the contained panel), edit, status → Cancelada (visible), no delete affordance in the form (only presets + Cancelar/Guardar), lifecycle note present, reload-persist.
  - Payments + linking: Payment created (₲ 250.000, `₲` guaraní formatting with dot separators), appears under Client → Pagos, row opens edit, `Total recibido` = sum (₲ 325.000 after a second ₲ 75.000 payment; not a balance/debt). Linked one Session → `Pago registrado →` appears; clicking it activates the Pagos tab and reveals the exact Payment (`aria-current`, teal tint) **without** opening the Session editor; linked-session count 0→1→2 as two Sessions were linked to one Payment (1→N holds); reload preserves the relationship. No payment void/delete tested or present.
  - Quick Capture: Session and Payment persist under the selected Client; `+ Nuevo cliente` escape hatch preserves the in-progress draft and auto-selects the new Client; records land in the normal Session/Payment tables (no parallel semantics).
  - Responsive: desktop, 1024 tablet-landscape, 834 tablet-portrait, 375 phone — dark rail / collapsed rail / top strip, directory, contained tabs, Calendar (+ appointments), single and nested drawers (430px, footer actions in view), duration controls, forms, Session/Payment rows, contact links — **no page-level horizontal overflow anywhere**, no clipped controls.
  - Accessibility: record rows are keyboard-reachable `<button>`s with `aria-label`; **no nested interactive elements** (`button button` / `button a` = 0); `Pago registrado →` is a sibling, not nested; status always carries a text label; a fresh browser tab shows **zero console errors** after exercising the app (a stale `ReferenceError: order is not defined` seen once in a long-lived HMR tab is an artifact of a prior session's intermediate edit — `order` does not exist in the current source and the production build is clean).
- **Defects found:** none blocking. Non-blocking: (1) Quick Capture success copy reads "Sesión guardado" / "Pago guardado" (should agree in gender: "guardada"); (2) the compact `Llamar` / `WhatsApp` contact chips are ~19px tall — small as touch targets on a phone, though the phone number itself is also a `tel:` link. Neither fixed (out of scope for a verification pass).
- **Cleanup:** hard delete is not possible — the app exposes none, the `anon` role has **no `GRANT DELETE`** (`permission denied for table clients`, hint `GRANT DELETE ON practice_management.clients TO anon`), and no service-role key is available. Per protocol, disposable records were left and are all clearly marked `PMS-TEST-001`; the 4 test **clients** were retired via the documented `Inactivo` lifecycle (authorized `UPDATE` only) so they drop out of the default Activo directory view. The pre-existing "Agus Ch" client was not touched. **No DELETE grant, API, UI, migration, or lifecycle behaviour was added.**
- **Residual disposable data** (needs a future service-role `DELETE` to purge): clients `77854c12…` (Cliente Prueba), `94682405…` (Cita Cliente), `3e4b0d81…` (QC Cliente), `f3e0f121…` (probe) — all `inactivo`; appointments `e1590804…`, `e7991f16…`; sessions `2fa5a050…`, `f92543d9…`, `1b06488e…`, `7b477cc8…`; payments `85e70172…`, `189e797b…`.
- **Engineering:** `npm run build` passes; `git diff --check` clean; `git status` unchanged from the pre-flight baseline plus this doc/README reconciliation; no schema/migration/dependency/config/auth/RLS change; no stray files; unrelated parent items (`../TECHNICAL_SPEC.md`, `../Task manager/`) untouched.
- **Verdict: READY TO COMMIT.**
